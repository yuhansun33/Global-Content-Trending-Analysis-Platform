[project]
name = "global-content-trending-analysis"
version = "0.1.0"
description = "Real-time streaming analytics platform for global content trending analysis"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "kafka-python>=2.0.2",
    "pyspark>=3.5.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "ruff>=0.4.0",
]
